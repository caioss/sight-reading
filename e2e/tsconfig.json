{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    /* Playwright specs run under Node; keep the DOM lib for page.evaluate callbacks. */
    "types": ["node"]
  },
  "include": ["./**/*.ts", "../playwright.config.ts"]
}
